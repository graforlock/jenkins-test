pipeline {
    agent any

    parameters {
        string(defaultValue: "", description: 'K', name: 'B')
    }

    stages {
        stage('PrintParameter'){
            steps{
                sh 'echo ${B}'
            }
        }
    }
}
